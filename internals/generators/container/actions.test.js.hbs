import { defaultAction } from '../actions';
import { MODIFY_TASK } from '../constants';

describe('{{ properCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of MODIFY_TASK', () => {
      const expected = {
        type: MODIFY_TASK,
      };
      expect(defaultAction()).toEqual(expected);
    });
  });
});
